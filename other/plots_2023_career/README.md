Please install with conda, e.g.

`$CONDA_PREFIX/bin/conda env create -n YOUR_PREFERRED_NAME --file environment.yml`

Please also install (manually since we're probably going to change them)
- nuflux
- ehefluxes